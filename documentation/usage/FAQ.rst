FAQ
*****


* | Q: I see a lot of postgres errors when installing the geoportal, is this a problem?
  | A: There are some errors in the postgres update sql files which do no harm to the installation. Errors coming from files named like "update_x.x.x_to_y.y_pgsql_UTF-8.sql" are probably okay

